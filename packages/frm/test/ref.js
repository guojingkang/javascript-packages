/* eslint-env mocha*/
/* eslint-disable strict, no-console*/

const assert = require('assert');
const common = require('./fixtures/common');

describe('ref', function () {
  before(function () {
  });
  after(function () {
  });

  it('should resolve the ref field right');
  it('should not write the ref field to db');
});
