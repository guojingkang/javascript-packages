/* eslint-env mocha*/

require('./helper');

describe('kvc: error', function () {
  it('should throw if get and set empty key');
  it('should throw to del empty key');
  it('should deal with error set store');
});
